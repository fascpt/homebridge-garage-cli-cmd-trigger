{
  "pluginAlias": "GarageCliCmdTrigger",
  "pluginType": "accessory",
  "singular": false,
  "schema": {
    "title": "Garage CLI Command Trigger",
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "Garage CLI Command Trigger",
        "description": "Name of Garage to appear in Home app"
      },
      "autoCloseDelay": {
        "title": "Auto Close Delay",
        "type": "integer",
        "default": 0,
        "description": "Number of seconds after opening door will automatically close. 0 to disable."
      },
      "openCmd": {
        "title": "Open Command",
        "type": "string",
        "description": "CLI command that is executed when the state changes from closed to opened."
      },
      "closeCmd": {
        "title": "Close Command",
        "type": "string",
        "description": "CLI command that is executed when the state changes from opened to closed."
      }
    }
  }
}
